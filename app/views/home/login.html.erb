<div class="container homeContainer">
	<!-- Page Content goes here -->
	<br>
	<br>
	<br>
	<div class="col s3"></div>

 	<div class="col s6 login">
 		<%= image_tag("logo.png", :class => "responsive-img logo")%>
		<br><br>
		<%= image_tag("motto.png", :class => "responsive-img")%>

		<br><br><br><br>
		<% if current_user %>
			Welcome <strong><%= current_user.name %></strong>!
    		<%= button_to "Sign out", signout_path, id: "sign_out" %>
		<% else %>
			<%= button_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
		<% end %>
  	</div>

  	<div class="col s3"></div>
</div>

